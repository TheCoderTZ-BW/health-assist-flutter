name: HealthcareTZ
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  device_preview: ^1.1.0
  ionicons: ^0.2.2
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^2.0.1

flutter_launcher_icons:
    android: "launcher_icon"
    ios: true
    image_path: "assets/icon/play.png"
    min_sdk_android: 21



flutter:
  uses-material-design: true

  assets:
    - assets/
